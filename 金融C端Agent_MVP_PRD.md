# 知股：投资观点证据与反证助手 — 一期 PRD

版本：v2.0 · 2026-09-17。用户确认：一期以观点研究为核心，二期做自选股与持续记忆跟踪。
配套：[面客产品与代码级架构](金融C端Agent_面客产品架构_v2.md)。

本版替代 v1.1 的自选股 MVP 和“暂不接 DeerFlow”选型。以下为待实现要求，不代表产品已开发、已压测或具备公开运营条件。

## 1. 定位与核心价值

**用户粘贴一个投资观点，获得有出处的支持证据、反方论证、关键假设与证据缺口，帮助判断观点是否站得住。**

核心用户：看到研报结论、社交媒体观点或朋友推荐，希望核实依据的个人投资者。不是交易执行器，不靠生成相反观点制造“辩论感”。

最小闭环：
`提交观点 → 确认标的与期限 → 独立调查支持/反证 → 校验 → 阅读报告与原文 → 围绕证据追问`。

首期聚焦 A 股单公司观点；仅允许选择已接入数据源覆盖的证券。无法解析的标的或期限要求用户确认；跨市场、多公司、宏观长报告明确提示未覆盖。

## 2. 面客功能与页面

| 页面 | P0 功能 | 验收要求 |
|---|---|---|
| `/app/research/new` | 粘贴观点；显示提取的证券、期限、关键主张；用户确认后启动 | 不擅自把“明年”解释为用户未确认的年份；防重复提交 |
| `/app/research/:id` | 研究进度、取消；完成后展示报告和证据抽屉 | 刷新可恢复；显示真实阶段，不编造完成百分比 |
| 同一报告页 | 支持证据、最强反证、成立/失效条件、未知项、证据追问 | 每项重要事实和推断绑定证据；原文、时间、数据口径可查 |
| `/app/history` | 我的研究记录、状态、重新研究、删除 | owner 隔离；重新研究产生新版本，不覆盖旧报告 |
| `/admin/ai-settings` | 管理员配置模型、数据源、预算并测试启用 | 普通用户不可访问；密钥不回显 |
| `/admin/research-runs` | 脱敏运行记录、用量、失败原因 | 默认不展示用户完整观点、对话或 Prompt |

Vue 工程保留一套，但 C 端使用独立布局与路由；不展示管理员菜单、代码生成器、模型 Key 或 Agent 调试台。移动端可阅读、点击来源、查看状态。

P0 账户采用受邀测试账户，不开放匿名无限研究；正式自助注册、找回密码、反滥用完善后再公开放量。

## 3. 报告标准

报告依次展示：原观点与研究范围、核心判断、支持证据、最强反证、哪些条件会改变判断、未知项、来源列表与数据截止时间。

观点状态使用“得到支持 / 受到挑战 / 证据混合 / 证据不足”，不用未经校准的“胜率 90%”或“看多方获胜”。反方未找到证据时如实记录；不能由此认定原观点已被证明。

虚构验收案例：“收入增长 30%，所以一年内股价会上涨”。应分别检验收入增长事实、利润与现金流转化、估值是否已反映预期。即便增长属实，也不能自动推出股价上涨。

外部数据库和检索结果是证据来源，不是天然真值。没有可靠正文、指标口径不一致或数据过期时，应删去不受支持的结论并解释缺口。

## 4. 已选架构：GoSaaS + Eino + DeerFlow

| 层 | 负责 | 不负责 |
|---|---|---|
| GoSaaS：Vue + Go/Gin | C 端与管理后台、账户权限、研究任务、额度、配置、数据与报告持久化 | 不把后台表格当用户产品；不允许模型绕过业务 API |
| Eino：嵌入 Go 服务的库 | 唯一总工作流：观点拆解、两路研究调度、汇总、校验与发布条件 | 不再运行另一套会自行派生专家的 Supervisor |
| DeerFlow：内部 Python 研究服务 | 分别执行支持方、反证方的有界工具调用与调查 | 不部署其 Next.js 客户前端；不经营账户/计费/最终报告；不递归派子 Agent |
| 数据与计算服务 | 只读金融查询、正文检索、确定性计算、证据登记 | 不向模型开放任意 SQL、任意 URL 或数据库密码 |

**系统是有界多 Agent，但只有一个总调度层。**Eino 发起两个独立 DeerFlow 研究会话；每个会话关闭子 Agent 委派和长期记忆。独立研究结束后，由应用校验后发布，研究者不能自行发布报告。

Eino 本身不是第三个服务，也不是前端。部署边界为一个 Vue 应用、一个 Go 业务服务、一个内部 Python 研究服务及数据库；不拼接两套客户界面。具体目录和接口见架构文档。

保留 GoSaaS 是业务底座选择；本地检出未找到 LICENSE/NOTICE，商用和复制其特有代码前必须确认授权，不因它基于开源上游就默认全部可商用。

## 5. 数据与可信性

- 业务状态和报告由 Go 服务统一写入关系数据库；新部署建议 PostgreSQL。SQLite 仅作单实例本地验证，不作为已证明的面客容量方案。
- 对接一个金融数据供应商和一个正文检索来源即可起步；接口通过统一 Provider 隔离，不将网页摘要作为财务事实。
- 每份证据保留原文/数值、来源、发布日期、取得时间、报告期、单位和版本。历史分析不得使用当时尚未公开的数据。
- 支持与反证共享事实口径、隔离研究上下文；最终检查覆盖双方的证据，不按用户喜好删除反向材料。
- 程序检查引用、时间和关键数字；模型辅助检查语义支持。模型复核不是正确性的保证，需要标注案例验收。
- 长期投资偏好记忆关闭；本次任务上下文、报告历史、短期追问仍保留，受用户权限和删除规则控制。

数据供应商、账户权限、授权范围及真实连接测试是上线依赖。离线样本仅供开发，必须标“历史样本/离线”，不能冒充外部数据库已接通。

## 6. 后台统一设置

配置只在 GoSaaS 管理后台维护；不要求运营人员同时修改 Eino 和 DeerFlow 两套 YAML。

基础字段：配置名称、协议、Base URL、Model ID、API Key、用途。用途分“拆解/汇总/复核”和“研究”；首期可全部使用同一模型配置。

流程：保存草稿 → 分别测试 Go 与 Python 实际调用链 → 测试 JSON/工具调用与证据不足案例 → 启用不可变版本。旧任务冻结旧版本，新任务读取新版本；修改字段后重新测试。

密钥加密存储、只写不回显；服务端安全获取，不进入 Prompt、前端或日志。模型配置与数据源配置独立。工具列表、预算、端点限制由服务器执行，不能靠 Prompt 或 UI 隐藏。

## 7. 预算、失败与一期范围

研究为异步任务。起始设计上限：支持/反证各最多 4 次模型请求、6 次数据工具；全任务最多 14 次模型请求、12 次数据工具、执行 180 秒；排队最多 30 秒。所有重试算入预算；这些是待压测的限制，不是完成率或时延承诺。

超时、一方失败、数据不足时，可展示已校验的部分证据，但明确“研究未完成”，不生成完整性结论。取消先显示“取消中”，确认 worker 停止后才显示已取消；已有供应商请求不保证撤回或免费。

二期：自选股、确认式长期记忆、新资料触发重新验证、订阅通知。暂不做：交易执行、收益预测保证、无限辩论、任意代码执行、批量 PDF/OCR、开放模型市场。

## 8. 验收和交付阶段

一期验收至少覆盖：正常研究、证据不足、反方无有效证据、相反来源冲突、未来信息泄漏、数字/单位错误、注入指令、越权读取、预算耗尽、取消、重复提交、断线刷新、模型热切换隔离、密钥泄漏检查。

三个里程碑分别验收：
1. **技术样机**：真实打通 Vue → Go/Eino → Python/DeerFlow → 一种外部数据 → 报告；一天仅作为这一链路的时间盒，不保证整个产品完成。
2. **受邀内测**：账户隔离、限额、失败恢复、后台配置、证据质量测试、部署和备份可用。
3. **公开面客**：落实数据授权、隐私删除与留存、适用合规评估、容量和安全测试、监控与服务支持。

“有 GoSaaS 后台”“接上 DeerFlow”均不等于达到公开面客条件。本版交付为产品与代码级设计，尚未开发或完成端到端联调。
