# 参与知股开发

当前阶段只承诺 fixture 开发预览。请先查看 [路线图](ROADMAP.md)、[实现状态](app/IMPLEMENTATION_STATUS.md) 和对应 SPEC。

## 提交问题

Bug 请附上系统、版本/提交、最小复现步骤、预期行为与脱敏后的日志。不要上传真实 API key、Cookie、个人研究材料或数据库副本。新能力请说明用户场景、验收条件及数据来源。

## 提交代码

1. Fork 后从 `main` 创建小范围分支。
2. 一次 PR 解决一个明确问题；避免同时改动无关服务。
3. 涉及契约时同步 `app/contracts` 与 `handoff/contracts`，并说明兼容性。
4. 修改权限、预算、状态机或证据归属时，增加能复现原问题的回归测试。
5. 按 [开发指南](docs/development.md) 运行适用检查，填写 PR 模板。

建议提交类型：`feat:`、`fix:`、`docs:`、`test:`、`chore:`。标题写清行为变化，允许中文。AI 辅助贡献同样需要贡献者理解代码、披露未验证边界并承担审查责任。

## 工程约束

- Go 拥有权限、业务状态与证据登记权；研究执行器不能自报可信证据。
- 不增加任意 shell、文件系统、浏览器或 MCP 工具来绕过工具白名单。
- 不能把 fixture、未实际执行的上游请求或缺失 usage 标成真实成功。
- 不提交私有参考仓库、环境文件、密钥、数据库、依赖缓存或本地审查产物。
- 未完成能力记录在路线图；界面和 README 不能提前宣传。

原创代码暂未指定开源许可证。提交改动前请阅读 [许可说明](LICENSE.md)；较大外部贡献应先与维护者明确授权范围。
