# 示例说明

全部为虚构 fixture，https://example.invalid 是故意不可访问的占位域名。不得拿这些内容展示真实投资价值。
四个 JSON 是消息结构样例，不是已经执行的端到端研究，也不是完整的双角色运行记录。
固定历史时间用于可重复测试；真实任务需在运行时生成有效 deadline。
report 的 completed+insufficient 表示流程可以完成但结论证据不足；不表示只要支持方返回就可发布。
Schema 验证与应用授权、数据真假、语义蕴含检查是不同层次。
